import React from 'react'
import { View, Text } from 'react-native'

const ChatRoom = (props) => {

    console.log(props.navigation, '/////////////////')

    return (
        
        <View>
            {
                // title = props.navigation.state.params.partnerUsername
            }
            <Text></Text>
        </View>
    )
}

export default ChatRoom
